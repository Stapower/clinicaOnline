import { AnimarDirective } from './animar.directive';

describe('AnimarDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimarDirective();
    expect(directive).toBeTruthy();
  });
});
