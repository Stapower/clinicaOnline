import { IsTurnoTakenDirective } from './is-turno-taken.directive';

describe('IsTurnoTakenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsTurnoTakenDirective();
    expect(directive).toBeTruthy();
  });
});
